This folder contains scripts that do not contribute to the rendering of the website. These scripts were used when migrating or importing data from external sources to the this website. 

- `pub_bibtex2json.py`: This Python script parses through a BibTex file exported from Zotero and retrieves paper information to generate a JSON file that is consistent with the data format used in `src/assets/data/publications.json` 

